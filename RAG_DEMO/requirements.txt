# 按你实际选用的方案取消注释并安装。
# 目标：能本地跑通 embedding + 向量检索 + LLM 调用即可。

# --- 环境变量 ---
python-dotenv>=1.0.0

# --- HTTP / OpenAI 兼容 API（推荐先用云端 embedding + chat）---
# openai>=1.40.0
# httpx>=0.27.0

# --- 本地向量检索（二选一即可）---
# numpy>=1.26.0
# faiss-cpu>=1.8.0
# chromadb>=0.5.0

# --- 可选：本地 sentence-transformers embedding ---
# sentence-transformers>=3.0.0
